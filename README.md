
  # Upload text file

  This is a code bundle for Upload text file. The original project is available at https://www.figma.com/design/g8ehvDiVJDT3ZcI04DSezn/Upload-text-file.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  